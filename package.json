{
  "name": "postcss-add-if",
  "version": "0.0.0",
  "description": "PostCSS plugin that converts declarations to use the if function.",
  "keywords": ["postcss", "css", "postcss-plugin", "postcss-add-if"],
  "scripts": {
    "unit": "node --test index.test.js",
    "test": "npm run unit && eslint ."
  },
  "author": "Vlad Negara <150728638+VladNegara@users.noreply.github.com>",
  "license": "MIT",
  "repository": "VladNegara/postcss-add-if",
  "engines": {
    "node": ">=18.0.0"
  },
  "peerDependencies": {
    "postcss": "^8.4.27"
  },
  "devDependencies": {
    "eslint": "^8.47.0",
    "postcss": "^8.4.27"
  },
  "eslintConfig": {
    "parserOptions": {
      "ecmaVersion": 2017
    },
    "env": {
      "node": true,
      "es6": true
    },
    "extends": ["eslint:recommended"]
  }
}
